# Rust / cargo build output
target/

# Staging tree for the .deb build (release.yml's build-linux-deb job)
deb-payload/

# Claude
.claude/
CLAUDE.md
plans/
docs/superpowers/specs

# Git worktrees (local scratch, never committed)
.worktrees/
docs/superpowers

---
status: proposed
date: YYYY-MM-DD
decision-makers: pbotcli maintainers
---

# ADR-NNNN: {Short title of the decision}

> **Historical reference.** This ADR records a decision made for the Python implementation
> that has since been removed from this repository. It is kept for history; the current CLI is
> the Rust line under `crates/`.

## Context and Problem Statement

{What forces, constraints, or pain points prompted this decision? Two or three paragraphs.
Include the concrete symptom that made it urgent — a broken command, a wrong exit code, a
mismatch between the SDK and the live API. Write it so a reader who never saw the original
issue can follow: the ADR must stand on its own.}

## Considered Options

- **A. {Option}** — {one line}
- **B. {Option}** — {one line}
- **C. {Option}** — {one line}

## Decision Outcome

Chosen option: **{A}**, because {the main driver}.

{Then the contract: the concrete rules this decision imposes on the code — bullet form,
each one checkable by a reviewer.}

## Confirmation

{Where the decision is enforced: the module or function that implements it, the tests that
cover it, or the review checklist item that catches violations.}

## Consequences

**Positive:**
- {…}

**Negative:**
- {…}

**Neutral:**
- {…}
